# Smokin Dudes Records — Artist shop

Next.js artist merch shop.

Artist photos use API CDN fields (`bannerUrl`, `avatarUrl`, `faviconUrl`). Printify product images stay on Printify hosts.

`next.config` allows `cdn.amc-dev.com`.
